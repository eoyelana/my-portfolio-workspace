export type MethodologyStep = {
  title: string;
  description: string;
};

export type CodeSnippet = {
  label: string;
  language: string;
  code: string;
};

export type CaseStudy = {
  title: string;
  problem: string;
  approach: string;
  result: string;
};

export type ProjectHeader = {
  eyebrow: string;
  title: string;
  intro: string;
};
